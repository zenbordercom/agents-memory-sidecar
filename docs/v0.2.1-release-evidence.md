# v0.2.1 Release Evidence

Date: 2026-07-17
Status: published and verified

## Release Identity

- Package: `agents-memory-sidecar@0.2.1`
- Git tag: `v0.2.1`
- Commit: `f13f17e90b8062f522d8bcf4b07477eeded69f7c`
- GitHub Release asset: `agents-memory-sidecar-0.2.1.tgz`
- Asset size: 51,491 bytes
- SHA256: `7eab33d63268a7d7f39dec3186e3f925e900b21a4e52e0fb3b975c9ee2c44231`
- npm shasum: `f101d72513d877fda5fe42015814f8014b4fc368`
- npm integrity: `sha512-T3uzyQVA8k+wgoXdtXriC+G3NECd9Ba+L9mtocQsCwo1zv0O4ThazTO2tFeZlEQ+ORGMwiW8EKQgrA3IaeB1GQ==`

The existing GitHub Release tarball was published directly. It was not rebuilt
from the later dirty working tree.

## Registry Verification

The following public metadata checks returned `0.2.1`:

```bash
npm view agents-memory-sidecar version
npm view agents-memory-sidecar dist-tags.latest
```

The public registry version list contains:

```text
0.1.0
0.1.1
0.2.1
```

## Clean Installation Verification

A temporary project installed the package from the public npm registry:

```bash
npm install agents-memory-sidecar@0.2.1
```

Verification confirmed:

- Installed package version is `0.2.1`.
- `agents-memory` resolves to `dist/cli.js`.
- `agents-memory-mcp` resolves to `dist/server.js`.
- `agents-memory-http` resolves to `dist/http-server.js`.
- `agents-memory --help` lists the expected memory commands.
- A fake-store `memory_add` call was accepted.
- A following `memory_search` returned the added item.
- The clean installation reported no known dependency vulnerabilities.

## Known Follow-Up

- Version 0.2.1 retains the documented local HTTP fallback actor when no token
  registry is configured. The v0.3.0 plan makes fail-closed authentication the
  next P0 runtime change.
- The repository license file still needs the complete Apache License 2.0 text
  and a confirmed copyright-holder identity.

No npm credentials, bearer tokens, database credentials, or private deployment
values are included in this record.
