# Grok Integration

Create a private env file and launcher from `config/agent-token.env.example` and `config/agents-memory-launcher.sh.example`.

Example:

```bash
grok mcp add agent-memory -- ~/.config/agents-memory/launchers/grok.sh
```

See `mcp_config.toml` for a TOML-style reference.
